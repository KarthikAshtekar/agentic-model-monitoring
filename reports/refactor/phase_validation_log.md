# Adapter-based refactor phase validation log

This log records the bounded checks used during the credit-preserving, diabetes-onboarding
refactor. Commands used the existing repository `.venv`; no source repository was
modified and no live credit case was rerun.

| Phase | Commands run | Result | Failures found and fixes applied | Final gate |
|---|---|---|---|---|
| 0 — baseline and protection | `git status --short`; `git log --oneline -5`; SHA-256/size capture and final comparison | 54 captured authoritative credit files remained unchanged; the required 22-file preservation subset verifies `22/22` | The requested persisted manifest was missing from the first handoff; created `credit_preservation_manifest.json` from the pre-refactor capture | Pass |
| 1 — registry and manifests | `python scripts\list_registered_models.py`; focused registry tests | Default remains `credit_default`; both strict manifests load; unknown IDs fail | Registry table omitted `feature_count`; added the manifest-derived column | Pass |
| 2 — binary adapter | Focused adapter/bundle tests; credit probability reproduction | Credit loads through `RegisteredModelBundle`; probabilities reproduce; order/leakage checks reject invalid input | None | Pass |
| 3 — domain packs | Domain policy/adversarial tests | Credit and diabetes terminology/actions remain separated; prohibited claims fail deterministically | Policy YAML used compatibility-oriented names; aligned persisted fields to `prediction_unit_wording`, `safe_terminology`, `required_uncertainty_language`, and `domain_limitations` while retaining read-only compatibility properties | Pass |
| 4 — remove credit assumptions | Focused monitoring/scenario/agent/evaluation tests; legacy hash checks | Default and explicit credit CLI paths remain supported; new outputs are model-namespaced | Legacy scenario tests initially expected a simplified feature-drift candidate list; corrected the demonstrably inaccurate fixture to the authoritative preserved candidates | Pass |
| 5 — deterministic inspector | `inspect_model_project.py` on diabetes and credit sources; inspector tests | Diabetes: 94 candidates; credit: 133 candidates; JSON/Markdown/YAML outputs created without deserialization or notebook execution | Eager package exports caused a circular import; made exports lazy. Added `.toml` discovery so `pyproject.toml` is visible | Pass |
| 6 — diabetes discovery | Read-only source/status/artifact inspection | Selected user-confirmed `25BM6JP22` at clean commit `7016f3f…`; fitted binary pipeline, calibrator, held-out data, predictions, metrics, model card, and environment identified | No comparable alternative was used; remembered metric hints were not treated as evidence | Pass |
| 7 — diabetes onboarding | `onboard_binary_classifier.py`; generic 15-check bundle validation; five-row inference smoke | `live_inference`; 50,736 rows; 21 raw/52 transformed features; scores and metrics reproduced | Initial `1e-7` gate missed by `6.64e-9`; set explicit `2e-7` sub-micro tolerance. Removed machine-specific source path from persisted bundle metadata | Pass |
| 8 — diabetes scenarios | `generate_monitoring_scenarios.py --all --model-id diabetes_risk`; `run_monitoring.py --all --model-id diabetes_risk` | Six reproducible scenarios; normal stable, DQ blocked, performance detected, unlabelled/partial-label cases abstained | First CLI invocation omitted required `--all`; no files were written; reran once with the explicit scope | Pass |
| 9 — cross-domain agent | Fake provider across six diabetes cases; policy, graph, fallback, approval and adversarial tests | Six fake cases passed with zero fallback/revision; normal avoided approval and five non-normal cases resumed approved | None | Pass |
| 10 — live diabetes Groq | Six model-specific live CLI invocations, reviewer `cross-model-evaluation` | All six completed; four live recommendations parsed; grounding/policy/approval all passed | Feature-drift and unlabelled recommendations received HTTP 429; unlabelled triage was also incompatible. Safe fallbacks were preserved; no live rerun or prompt tuning | Pass with documented provider limitations |
| 11 — evaluation | `evaluate_live_agent.py --model-id diabetes_risk`; `evaluate_cross_model_agent.py` | Diabetes and cross-model JSON/Markdown/CSV generated; no predictive metrics averaged | Diabetes report initially inherited credit defect history; made notes model-specific. Cross-model output omitted revision rate; added `8.33%` aggregate and focused regression test | Pass |
| 12 — documentation and claim audit | Documentation review, link/path/security scans | Required guides, ADR, onboarding reports, claim audit, CV wording and final report updated | Removed stale zero-fallback wording and machine-specific persisted paths | Pass |
| Final focused validation | Consolidated targeted pytest; requested targeted Ruff; `git diff --check`; credential/provenance/hash audits | 86 consolidated tests passed; targeted Ruff passed; `.env` ignored; API-key value absent from changed files; 12 diabetes fake/live reports had valid provenance; 54/54 broader credit hashes unchanged | Known joblib/NumPy deserialization deprecation warning retained as a documented compatibility limitation | Pass |

No phase exceeded three focused repair cycles. The two live fallbacks are evaluation
outcomes, not fabricated successes.
